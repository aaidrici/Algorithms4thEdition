package Chapter_2_5;

public class Chapter_2_5 {


    // 2.4.1 If the same two instances are compared, they must be identical. There is no point in evaluating the
    // individual characters until a difference is found.
    // 2.5.2 see CompoundWords
    // 2.5.3 It is flawed because it provides no information regarding the magnitude of the difference.
    //       Two alternative comes to mind:
    //          -> return this.amount - that.amount
    //          -> if (this.amount > that.amount - 0.005 && this.amount < that.amount + 0.005) return 0;
    //             else return Math.ceil(this.amount - that.amount);
    //
    // 2.5.4 see Dedup
    // 2.5.5 During the exchange process, an element 'C' at the pointer (splitting the sorted and unsorted
    //       sides of the array) may be swapped with an element 'B' that is on the right of an element with a key
    //       equal to 'C'. Hence the initial relative ordering of 'C's in the array is broken.
    // 2.5.6 see RecursiveSelect
    // 2.5.7
    // 2.5.8
    // 2.5.9
    // 2.5.10 see Version
    // 2.5.11

    // 2.5.12 see Scheduling
    // 2.5.13
    // 2.5.14 see LoadBalancing
    // 2.5.15
    // 2.5.16 see California
    // 2.5.17
    // 2.5.18
    // 2.5.19
    // 2.5.20
    // 2.5.21
    // 2.5.22
    // 2.5.23
    // 2.5.24
    // 2.5.25
    // 2.5.26
    // 2.5.27
    // 2.5.28
    // 2.5.29
    // 2.5.30

    // 2.5.31
    // 2.5.32
    // 2.5.33


}
