package Chapter_5_5;

public class Q5_5_12 {

    // If all the frequencies follow a distribution of decreasing order by a factor 2, the following Huffman code can
    // be expected:
    // -> The resulting trie should be skewed on one side and all leafs have no sibling, except for two left on the
    //    lowest level of the trie.
    // -> All character address should have a unique length, except for the two leafs at the bottom of the trie with
    //    the every of their bit of their bitcode identical except for the very last one.

}
